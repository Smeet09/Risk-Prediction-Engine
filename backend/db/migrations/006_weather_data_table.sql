-- Migration to add tabular storage for ERA5 aggregated 2km weather grids
CREATE TABLE IF NOT EXISTS weather_data (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  country TEXT NOT NULL,
  state TEXT NOT NULL,
  date DATE NOT NULL,
  lon FLOAT NOT NULL,
  lat FLOAT NOT NULL,
  grid_id TEXT NOT NULL,
  rain_mm FLOAT,
  max_intensity_mm_hr FLOAT,
  temperature_2m FLOAT,
  dewpoint_temperature_2m FLOAT,
  surface_pressure FLOAT,
  mean_sea_level_pressure FLOAT,
  wind_speed_ms FLOAT,
  wind_dir_deg FLOAT,
  soil_moisture_m3m3 FLOAT,
  surface_runoff_mm FLOAT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  
  -- Additional parameters found in existing data
  convective_precipitation_mm FLOAT,
  snowmelt_mm FLOAT,
  total_column_water_vapour_kg_m2 FLOAT,
  u10_ms FLOAT,
  v10_ms FLOAT,
  u100_ms FLOAT,
  v100_ms FLOAT,
  temperature_2m_k FLOAT,
  dewpoint_temperature_2m_k FLOAT,
  boundary_layer_height_m FLOAT,
  sea_surface_temperature_k FLOAT,
  mean_total_precipitation_rate_kg_m2s FLOAT,
  surface_net_solar_radiation_j_m2 FLOAT,
  potential_evaporation_m FLOAT,
  snow_cover_fraction FLOAT,
  mean_evaporation_rate_kg_m2s FLOAT,
  cape_j_kg FLOAT,
  cin_j_kg FLOAT,
  k_index_k FLOAT,
  cloud_base_height_m FLOAT,
  soil_moisture_layer1_m3m3 FLOAT,
  soil_moisture_layer2_m3m3 FLOAT,
  soil_temperature_level1_k FLOAT,
  lai_high FLOAT,
  lai_low FLOAT,
  relative_vorticity_850_s1 FLOAT,
  divergence_200_s1 FLOAT,
  vertical_velocity_500_pa_s FLOAT,
  specific_humidity_850_kg_kg FLOAT,
  relative_humidity_700_pct FLOAT,
  significant_wave_height_m FLOAT,
  peak_wave_period_s FLOAT,
  surface_runoff_m FLOAT,
  mean_sea_level_pressure_pa FLOAT,

  UNIQUE(state, grid_id, date)
);

CREATE INDEX IF NOT EXISTS idx_weather_data_state_date ON weather_data (state, date);
CREATE INDEX IF NOT EXISTS idx_weather_data_grid ON weather_data (grid_id);
